/// Compose feature-specific constants
class ComposeConstants {
  ComposeConstants._();

  // ---------- UI Constants ----------
  static const double postingIndicatorSize = 16.0;
  static const double postingIndicatorStrokeWidth = 2.0;

  // ---------- Feature-Specific Validation ----------
  static const int maxPhotoCount = 10;
  static const int maxTagCount = 20;

  // Note: postingDelaySeconds, postingSuccessDelayMs, and maxLocationLength
  // are now available in AppConstants from core/constants.dart
}
